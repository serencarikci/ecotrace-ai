# Anomaly Detection

Deterministic methods (no ML required): rolling mean/std, z-score, IQR, percentage-change, expected frequency / missing data, rule consistency checks.

Events include evidence JSON, severity mapping, fingerprint deduplication. Detection never modifies source activity/inventory records. Dismissal requires a reason.
