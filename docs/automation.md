# Automation

Organization-scoped rules with templates (weekly anomaly scan, monthly report, daily DQ, target risk, import failure, quarterly supplier review, regulatory effective-date alert).

Activation requires permission; schedules validate; execution keys prevent duplicates; destructive actions prohibited; runs are auditable.
